📋 前置流程
读取目标 Vue 组件文件结构，理解现有 DOM 布局与类名规范
明确组件定位为移动端或PC端组件，需遵循项目组件统一规范
识别基础样式框架（如 Tailwind/CSS 变量体系）
理解CSS代码规范，为容器尺寸优化做准备

🔄 分阶段执行流程
阶段 1：模块格式转换（唯一即时操作）
将 CMD 引入语法转为 ESM 规范（仅修改 import/export 语句）
输出文件命名为 index_esm.vue（在该文件上进行后续修改）
严格禁止修改现有类名、样式引用或组件调用方式

阶段 2：双轨分析（纯分析，不生成代码）
设计图分析：
▶ 提炼界面功能模块与视觉层次
▶ 标注交互元素与样式特征（如主色、间距规则）
▶ 识别需统一的组件类型（如图标、按钮、图片）
代码分析：
▶ 验证现有 DOM 与设计图的布局匹配度
▶ 检查类名是否符合语义化规范（如btn-primary而非c1）
▶ 定位未使用统一组件的位置（如原生 img 标签未用封装组件）
▶ 检查容器尺寸设置是否符合CSS规范，识别可优化的布局（如margin居中的元素）

阶段 3：分层实现方案设计（纯文本方案）
组件统一方案：
▶ 制定组件替换映射表（如img→l-img，i→Icon）
▶ 规范图标资源路径（统一使用@/assets/icons/）
▶ 多语言文本替换策略（{{text}}→$t('key')）
样式规范方案：
▶ 类名语义化改造计划（如menu-item替代item-1）
▶ 主题色 CSS 变量替换方案（#ff0000→var(--primary)）
▶ 响应式布局适配策略（媒体查询 / 弹性布局）
▶ 容器尺寸优化方案：
   ● 最外层容器设置为100%宽度
   ● 将使用margin居中的元素改为flex布局
   ● 遵循CSS代码规范调整其他容器尺寸
技术方案说明：
▶ 组件统一与样式改造的实现顺序
▶ 避免破坏现有布局的技术要点
▶ 容器尺寸优化的具体实施步骤和注意事项

阶段 4：代码生成（需指令触发）
触发条件：收到 “开始编码” 明确指令
实现原则：
▶ 组件统一：
● 按方案替换所有基础组件（保持 DOM 结构不变）
● 统一图标 / 图片组件的属性参数（如加载占位符）
● 集成多语言语法并保留原始文本作为 key 注释
▶ 样式规范：
● 按语义化规则重命名类名（需保留原样式映射关系）
● 替换硬编码颜色为项目主题变量
● 补充响应式类名（如md:px-4）而不修改原始布局
● 按CSS规范调整容器尺寸，将margin居中元素改为flex布局
▶ 代码注释需标注组件 / 样式改造对应的设计图区域及容器尺寸优化的部分

🚫 核心约束
组件替换必须保持原始 DOM 层级与属性不变
样式改造不得影响现有布局尺寸（仅修改类名 / 颜色变量 / 布局方式）
容器尺寸优化需遵循CSS代码规范，确保不破坏现有布局
统一组件的引入需遵循 ESM 规范（阶段 1 转换后的格式）
禁止自动创建新组件文件，仅在index_esm.vue内修改组件调用方式和样式

📌 规范优先级
布局保护：DOM 结构不变 > 组件统一 > 样式优化
样式一致性：主题色变量集成 > 类名语义化 > 响应式适配 > 容器尺寸规范
组件标准化：基础组件替换 > 资源路径统一 > 多语言集成

关键调整说明
规范流程化：将组件与样式规范拆解为分析→方案→实施三阶段任务
约束显性化：明确组件替换不改变 DOM 结构、样式改造不影响布局等核心原则
技术抽象化：使用 “统一组件”“主题变量” 等通用术语替代具体组件名（如 l-img）
风险防控：在方案阶段提前规划组件 / 样式改造的兼容性方案，包括容器尺寸优化的兼容性