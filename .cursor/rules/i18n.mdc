---
description: 
globs: 
alwaysApply: true
---
# 多语言开发说明

# 你生成的所有代码均要遵循此说明，避免硬编码；生成的代码需要参考第二点的文件结构，并添加对应的json结构；删除代码的时候如果遇到多语言，也需要审查文件结构，并删除对应的键值对。

## 1. 概述

项目的多语言应该基于i18n插件，尽可能的使用框架原本的方法。

## 2. 文件结构

```
src/language/
├── index.js      # 多语言系统的主要逻辑
├── en.json       # 英文语言包
```

## 3. 配置说明

当前系统配置：
- 默认语言：英文(en)
- 配置支持语言：英文(en)
- 自动检测浏览器语言，优先使用浏览器语言(如浏览器是英文则默认使用英文,没有对应的语言包则使用默认语言)


## 4. 翻译键值结构规范

使用多语言时，先检查是否已有重复值，如无则新建
使用扁平化的键值结构，避免嵌套对象。
键名应根据语义简洁命名，如 login、submit，无需冗余前缀。
**语义命名优先，仅在存在上下文冲突或模块语义明确时，使用 . 分隔命名空间**
例如：
user.placeholder（表示用户模块中的占位符）
form.error.required（表示表单模块中的必填错误提示）
保持命名一致性、可读性强，便于维护与复用。
示例
```json
{
  "login": "Login",
  "submit": "Submit",
  "cancel": "Cancel",
  "user.placeholder": "Enter your name",
  "form.error.required": "This field is required"
}
```


## 5. 参数化翻译

支持使用大括号{param}格式的参数化文本：

```
// 语言包定义
"welcome": "欢迎, {name}!"

// 使用方式
t('common.welcome', { name: '用户' }) // 输出：欢迎, 用户!
```

## 6. 添加新语言包步骤

1. 在`src/language`目录下创建新的语言包文件，例如`zh.json`
2. 将该语言包导入到`index.js`中：
   ```js
   import zh from './zh.json';
   
   // 更新语言包映射
   const languageModules = {
     en,
     zh
   };
   
   // 更新支持的语言列表
   const supportedLanguages = ['en', 'zh'];
   ```

3. 确保在`main.js`中配置了该语言：
   ```js
   app.use(i18nPlugin, {
     defaultLang: 'en',
     languages: ['en', 'zh']
   });
   ```

## 7. 最佳实践

1. **翻译键命名**：使用单层结构，命名语义化或模块语义化
2. **保持一致性**：确保所有语言包包含相同的键
3. **组合方式**：优先使用组合式API，便于TypeScript类型支持
4. **默认值处理**：当翻译不存在时，系统会返回键名作为默认值
5. **避免硬编码**：所有界面文本都应使用翻译函数，避免硬编码文本
6. **不要生成中文json**：当我提问的时候使用中文，你不应该为我额外生成zh.json

