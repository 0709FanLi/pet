---
description: 
globs: 
alwaysApply: true
---
# Vue3 Composition API 代码规范指南

---

## 1. 概述
- 本规范基于Vue3 Composition API和TypeScript，旨在确保代码的一致性、可读性和可维护性。
- 单文件组件（SFC）采用 `<script setup>` 与 `lang="scss"`
- 全局文本用 **英文**；注释用 **中文**
- 多语言遵循 `src/language` 目录规范（扁平键名）

---

## 2. 文件结构规范

### 2.1 SFC 章节顺序
```vue
<template> … </template>

<script setup>
  // 1. 引入区域
  // 2. props 区域
  // 3. emits 区域
  // 4. 变量区域
  // 5. 计算属性区域
  // 6. watch 区域
  // 7. 点击事件区域
  // 8. 自定义事件区域
  // 9. 业务逻辑区域
  // 10. 数据解析区域
  // 11. 初始化方法区域
  // 12. 周期函数区域
  // 13. 导出区域
</script>

<style scoped lang="scss"> … </style>
```

---

## 3. 命名规范（集中说明，所有模块引用本章）
| 类型 | 规则 | 规则示例 |
| ------- | ------- | ------- |
| `props` | `[助动词(可选)][上下文][数据类型(可选)]` | `isLoading`, `hasError`, `tokenList` |
| `emits` | `on[上下文][事件动词]` | `onCreateListener`, `onUploadFinish` |
| `变量` | `[助动词(可选)][上下文][数据类型(可选)]` | `isLoading`, `hasError`, `tokenList` |
| `常量` | 全大写下划线分隔 | `API_BASE_URL`, `DATE_FORMATS` |
| `计算属性` | `[状态(可选)][上下文][属性]` | `inputStyle`, `isBtnDisabled`, `cameraWidth`, `giftSrc` |
| `点击事件` | `click[上下文]` | `clickUserAvatar`, `clickDownloadFile` |
| `自定义事件` | `on[事件动词][上下文]` | `onTouchStratAvatar`, `onScrollMessage`,  `onChangeNickname` |
| `业务逻辑区域` | `[逻辑动词][上下文][事件动词]` | `handleImageUpload`, `executeMessageQuery` |
| `数据解析区域` | `[解析名次][上下文]` | `parseUserData`, `formatTime`, `filterListId` |
| `初始化方法` | `init[对象]` | `initView`, `initData`, `initEvent` |
> 上下文：指触发场景、数据来源、功能模块等
---

## 4. `<script setup>` 详细规范
（各小节 *就近* 引用命名规范，避免重复）

### 4.1 引入区域
```ts
// Composable
import { useLogger } from '@/utils/useLogger';
// 组件
import LImg from '@/components/l-img.vue';
// Vue 对象
import { ref, reactive, computed, watch, onMounted } from 'vue';
import { useI18n } from 'vue-i18n';
import { useUserStore } from '@/store/modules/user';
// 功能模块
import { ApiCharacter } from '@/api/api-character';
// 常量
import { DATE_FORMATS } from '@/types/date.ts';
// 工具方法
import { convertUtcToLocal } from '@/utils/util';
```
> 引入顺序：Composable → 组件 → Vue 对象 → 功能模块 → 常量 → 工具方法


### 4.2 props区域
> 命名规则见 **3-命名规范（集中说明，所有模块引用本章）**


```ts
const props = defineProps({
  visible: { type: Boolean, default: false }
});
```

### 4.3 emits区域
> 命名规则见 **3-命名规范（集中说明，所有模块引用本章）**

```ts
const emit = defineEmits(['onAvatarVisible', 'onCreateListener']);
```

### 4.4 变量区域
> 命名规则见 **3-命名规范（集中说明，所有模块引用本章）**

```ts
// 框架变量
const userStore = useUserStore();
const { t } = useI18n();
// Composable
const { logMessages, log } = useLogger();
// 常量
const genderOptions = [{ value: "genderless", label: t("genderless") }];
// 业务逻辑变量
let errorCount = 0;
// 响应式变量
const isChange = ref(true);
const someObj = reactive({ title: 'hello' });
```
> 声明顺序：框架变量 → Composable → 常量 → 业务逻辑变量 → 响应式变量

### 4.5 计算属性区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**

```ts
const avatarStyle = computed(() => (isChange.value ? { color: 'red' } : {}));
const isButtonDisabled = computed(() => !isChange.value);
```

### 4.6 watch 区域
```ts
watch(isChange, newVal => {});
```

### 4.7 点击事件区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**

```ts
const clickChange = () => {
  isChange.value = !isChange.value;
};
```

### 4.8 自定义事件区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**

```ts
const onLoadImg = () => {};
```

### 4.9 业务逻辑区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**
> 包含：逻辑判断、增删改查、提交、上传、验证等

```ts
const handelChangeAction = () => {
  someObj.title = isChange.value ? 'hello2' : 'hello';
};
```

### 4.10 数据解析区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**
> 包含：数据解析、数据转换、数据格式化等

```ts
const parseData = () => {};
```

### 4.11 初始化方法区域
> 命名规则见  **3-命名规范（集中说明，所有模块引用本章）**
> 包含：初始化视图、变量赋值、初始化事件等初始化操作

```ts
const initData = () => { /* 变量赋值… */ };
const initView = () => { /* 网络请求，渲染UI */ };
const initEvent = () => { /* 事件监听 */ };
```

### 4.12 周期函数区域
```ts
// 更新阶段
onUpdated(() => {});
// 创建阶段
onMounted(() => {/* 执行初始化方法 */});
// 销毁阶段
onUnmounted(() => {});
```
> 声明顺序：更新阶段 → 创建阶段 → 销毁阶段

### 4.13 导出区域

```ts
defineExpose({
  handelChangeAction
});
```

---

## 5. 样式与资源
> 本章节内容详见 [styles-coding.mdc](mdc:.cursor/rules/styles-coding.mdc)
> 命名规则见 **3-命名规范（集中说明，所有模块引用本章）**

---

## 6. 代码质量要求
- 每行结尾加 `;`
- 只用 `const` / `let`，禁止 `var`
- 优先使用 `async/await` 处理异步
- `console.log` 内容要有意义的描述

---

## 7. AI 生成代码检查清单
- [ ] 严格遵守 **2.1-SFC 章节顺序**
- [ ] 所有命名符合 **3-命名规范（集中说明，所有模块引用本章）**
- [ ] Template 属性顺序正确  **5.1 属性书写顺序**
- [ ] Style 使用 `scoped` & `scss`
- [ ] 导入顺序正确
- [ ] LifeCycle 声明顺序：**4.12 周期函数区域**
- [ ] 结尾分号齐全

---

## 8. 最佳实践
1. **保持一致性**：所有文件遵循本规范
2. **模块化**：复用逻辑抽取为 Composable 或组件
3. **注释适度**：区域与复杂逻辑必须注释
4. **类型安全**：充分利用 TypeScript
