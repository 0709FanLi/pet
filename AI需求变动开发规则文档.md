# 前端项目需求变动AI开发规则文档

## 项目背景
本文档专门针对已完工的前端项目(fc_admin)在遇到需求变动时，使用AI进行开发的规范和规则。项目基于uni-app Vue3框架，支持多端开发。

## 核心原则

### 1. 代码一致性原则
- **严格遵循现有代码风格**：保持与项目现有代码的命名规范、文件结构、组件设计模式一致
- **复用现有组件**：优先使用项目中已有的组件库和工具函数，避免重复造轮子
- **遵循现有架构模式**：延续项目的分层架构、状态管理、路由管理等模式

### 2. 影响最小化原则  
- **精准定位修改范围**：仅修改需求变动直接相关的代码，避免不必要的重构
- **保持向下兼容**：新增功能不能破坏现有功能，确保已有业务逻辑正常运行
- **渐进式改进**：分步骤实施变更，每个步骤都要可回滚

### 3. 质量保障原则
- **完整性检查**：确保新增功能的完整实现，包括UI、逻辑、数据处理、错误处理
- **边界情况处理**：考虑各种异常情况和边界条件
- **性能优化意识**：避免引入性能问题，复用现有优化策略

## 开发规范

### 技术栈规范
```javascript
// 项目技术栈
- 框架：uni-app Vue3 (Composition API)
- 样式：SCSS + 原子化CSS类
- 状态管理：Vuex
- 国际化：多语言支持 (language/ 和 locale/ 目录)
- 组件库：自定义组件 + uni-ui
- 原生插件：支持Android/iOS原生模块
```

### 文件结构规范
```
新增功能时严格按照以下目录结构：
├── components/          # 可复用组件
├── pages/              # 页面文件
├── pagesPub/           # 公共业务页面
├── common/             # 工具函数和通用逻辑
├── comCss/             # 样式文件
├── const/              # 常量定义
├── language/           # 国际化资源
└── store/              # 状态管理
```

### 代码风格规范
1. **组件命名**：使用kebab-case，如`user-card.vue`
2. **样式类名**：遵循现有原子化CSS规范，如`.btn-main`, `.c-3`, `.bg-second`
3. **变量命名**：使用camelCase，保持语义化
4. **文件命名**：遵循现有命名模式，如`com*.js`表示通用组件

### 开发流程规范

#### 需求分析阶段
1. **影响评估**
   - 分析需求对现有功能的影响范围
   - 识别需要修改的文件和组件
   - 评估是否需要新增页面/组件

2. **技术方案确定**
   - 选择最小侵入式的实现方案
   - 确定复用哪些现有组件和工具
   - 规划新增代码的位置

#### 实现阶段
1. **代码修改顺序**
   ```
   第一步：修改/新增基础组件
   第二步：修改/新增业务逻辑
   第三步：修改/新增页面结构
   第四步：更新样式和国际化
   第五步：更新路由和状态管理
   ```

2. **代码实现要求**
   - 新增组件必须包含完整的props定义、emits定义、错误处理
   - 新增页面必须包含正确的生命周期处理
   - 必须正确处理多语言和主题适配
   - 必须考虑不同端的兼容性

#### 质量检查阶段
1. **功能验证**
   - 新功能完整性测试
   - 现有功能回归测试
   - 边界条件和异常情况测试

2. **代码质量检查**
   - 代码风格一致性检查
   - 性能影响评估
   - 内存泄漏风险评估

## 常见需求变动场景处理指南

### 场景1：新增页面
```javascript
// 处理步骤：
1. 在对应的pages目录下创建页面文件
2. 在pages.json中注册路由
3. 创建对应的组件和样式文件
4. 更新导航和状态管理
5. 添加国际化资源
```

### 场景2：修改现有功能
```javascript
// 处理原则：
1. 先备份原有实现
2. 采用参数化或配置化方式扩展功能
3. 保持原有API接口不变
4. 添加版本兼容处理
```

### 场景3：新增组件
```javascript
// 组件设计要求：
1. 必须支持主题切换
2. 必须支持多语言
3. 必须有完整的prop类型定义
4. 必须有清晰的事件定义
5. 必须考虑不同端的适配
```

### 场景4：API接口变更
```javascript
// 处理策略：
1. 在common/api.js中统一管理
2. 保持原有接口封装不变
3. 新增接口要遵循现有错误处理机制
4. 更新catch和upload相关处理
```

## 禁止操作清单

### 绝对禁止
- ❌ 删除或重命名现有公共组件
- ❌ 修改全局样式变量定义
- ❌ 改变现有组件的公开API
- ❌ 修改现有数据库结构处理逻辑
- ❌ 改变现有的路由结构和命名

### 谨慎操作
- ⚠️ 修改store中的state结构
- ⚠️ 修改全局配置文件
- ⚠️ 修改原生插件调用逻辑
- ⚠️ 修改socket连接和消息处理机制

## 测试验证要求

### 功能测试
1. **多端测试**：Android、iOS、H5端功能一致性
2. **多语言测试**：所有支持语言的正确显示
3. **主题测试**：深色/浅色主题适配
4. **权限测试**：不同角色权限下的功能表现

### 性能测试
1. **内存使用**：确保没有内存泄漏
2. **渲染性能**：页面加载和切换流畅度
3. **网络请求**：API调用效率和错误处理
4. **数据处理**：大数据量情况下的处理能力

### 兼容性测试
1. **版本兼容**：新老版本数据格式兼容
2. **设备兼容**：不同屏幕尺寸和分辨率适配
3. **系统兼容**：不同操作系统版本支持

## 文档更新要求

### 必须更新的文档
1. **组件文档**：新增组件的使用说明
2. **API文档**：新增接口的调用方式
3. **配置文档**：新增配置项的说明
4. **部署文档**：如有部署变更需更新

### 推荐更新的文档
1. **架构文档**：如有架构层面变更
2. **开发指南**：新的开发模式或工具
3. **故障排查指南**：新增的错误处理机制

## 应急回滚机制

### 快速回滚策略
1. **代码回滚**：使用Git回滚到变更前版本
2. **配置回滚**：恢复修改前的配置文件
3. **数据回滚**：如有数据结构变更，准备回滚脚本

### 风险控制
1. **分步部署**：重大变更分阶段部署
2. **灰度发布**：新功能先小范围验证
3. **监控预警**：部署后密切监控关键指标

---

**重要提醒**：本规则文档的目标是确保需求变动的高质量交付，同时最大程度保护现有系统的稳定性。在执行任何修改前，务必充分理解现有代码逻辑和业务流程。 