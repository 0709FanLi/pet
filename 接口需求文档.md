# 宠物找回平台 - 接口需求文档

## 概述

本文档定义了宠物找回平台所需的所有后端接口，按功能模块划分，包含请求参数、响应格式和业务逻辑说明。

## 接口规范

### 基础信息

- **协议**：HTTPS
- **请求格式**：JSON
- **响应格式**：JSON
- **字符编码**：UTF-8

### 通用响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 状态码说明

- `200`：成功
- `400`：请求参数错误
- `401`：未授权
- `403`：禁止访问
- `404`：资源不存在
- `500`：服务器内部错误

## 1. 用户认证模块

### 1.1 用户注册

- **接口**：`POST /api/auth/register`
- **描述**：用户注册
- **请求参数**：

```json
{
  "phone": "13800138000",
  "password": "123456",
  "confirmPassword": "123456",
  "verificationCode": "123456",
  "userType": "owner", // owner: 宠物主人, team: 找寻团队
  "agreeTerms": true
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1,
    "token": "jwt_token_string",
    "userType": "owner"
  }
}
```

### 1.2 发送验证码

- **接口**：`POST /api/auth/send-code`
- **描述**：发送短信验证码
- **请求参数**：

```json
{
  "phone": "13800138000",
  "type": "register" // register: 注册, reset: 重置密码
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "验证码发送成功",
  "data": {
    "expireTime": 300 // 过期时间（秒）
  }
}
```

### 1.3 用户登录

- **接口**：`POST /api/auth/login`
- **描述**：用户登录
- **请求参数**：

```json
{
  "phone": "13800138000",
  "password": "123456"
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "token": "jwt_token_string",
    "userType": "owner",
    "userInfo": {
      "nickname": "用户昵称",
      "avatar": "头像URL",
      "phone": "13800138000",
      "isVerified": false // 是否实名认证
    }
  }
}
```

### 1.4 退出登录

- **接口**：`POST /api/auth/logout`
- **描述**：用户退出登录
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：

```json
{
  "code": 200,
  "message": "退出成功",
  "data": null
}
```

### 1.5 重置密码

- **接口**：`POST /api/auth/reset-password`
- **描述**：重置密码
- **请求参数**：

```json
{
  "phone": "13800138000",
  "verificationCode": "123456",
  "newPassword": "123456"
}
```

## 2. 用户信息模块

### 2.1 获取用户信息

- **接口**：`GET /api/user/profile`
- **描述**：获取当前用户信息
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1,
    "nickname": "用户昵称",
    "avatar": "头像URL",
    "phone": "13800138000",
    "userType": "owner",
    "isVerified": false,
    "balance": 1000.0,
    "createdAt": "2023-01-01T00:00:00Z"
  }
}
```

### 2.2 更新用户信息

- **接口**：`PUT /api/user/profile`
- **描述**：更新用户信息
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "nickname": "新昵称",
  "avatar": "新头像URL"
}
```

### 2.3 实名认证

- **接口**：`POST /api/user/verification`
- **描述**：提交实名认证
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "idCardFront": "身份证正面图片URL",
  "idCardBack": "身份证反面图片URL",
  "realName": "真实姓名",
  "idCardNumber": "身份证号码"
}
```

### 2.4 获取认证状态

- **接口**：`GET /api/user/verification-status`
- **描述**：获取实名认证状态
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "pending", // pending: 待审核, approved: 已通过, rejected: 未通过
    "reason": "审核意见",
    "submittedAt": "2023-01-01T00:00:00Z"
  }
}
```

## 3. 宠物丢失信息模块

### 3.1 发布丢失信息

- **接口**：`POST /api/pets/lost`
- **描述**：发布宠物丢失信息
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "photos": ["图片URL1", "图片URL2"],
  "lastLocation": "最后出现地点",
  "latitude": 39.9042,
  "longitude": 116.4074,
  "petBreed": "宠物品种",
  "petName": "宠物名字",
  "description": "详细描述",
  "rewardAmount": 500.0,
  "paymentMethod": "wechat" // wechat: 微信, alipay: 支付宝
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "发布成功",
  "data": {
    "postId": 1,
    "paymentOrderId": "order_123456",
    "paymentUrl": "支付链接"
  }
}
```

### 3.2 获取丢失信息列表

- **接口**：`GET /api/pets/lost`
- **描述**：获取丢失信息列表
- **请求参数**：

```
page=1&limit=10&status=all&userId=1
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "postId": 1,
        "photos": ["图片URL1"],
        "lastLocation": "最后出现地点",
        "petBreed": "宠物品种",
        "petName": "宠物名字",
        "rewardAmount": 500.0,
        "status": "pending", // pending: 待接单, accepted: 已接单, completed: 已完成
        "createdAt": "2023-01-01T00:00:00Z",
        "ownerInfo": {
          "nickname": "宠物主人昵称",
          "phone": "13800138000"
        }
      }
    ],
    "total": 100,
    "page": 1,
    "limit": 10
  }
}
```

### 3.3 获取丢失信息详情

- **接口**：`GET /api/pets/lost/{postId}`
- **描述**：获取丢失信息详情
- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "postId": 1,
    "photos": ["图片URL1", "图片URL2"],
    "lastLocation": "最后出现地点",
    "latitude": 39.9042,
    "longitude": 116.4074,
    "petBreed": "宠物品种",
    "petName": "宠物名字",
    "description": "详细描述",
    "rewardAmount": 500.0,
    "status": "pending",
    "createdAt": "2023-01-01T00:00:00Z",
    "ownerInfo": {
      "userId": 1,
      "nickname": "宠物主人昵称",
      "phone": "13800138000",
      "avatar": "头像URL"
    },
    "teamInfo": {
      "userId": 2,
      "nickname": "找寻团队昵称",
      "phone": "13800138001",
      "acceptedAt": "2023-01-01T00:00:00Z"
    }
  }
}
```

### 3.4 我的发布列表

- **接口**：`GET /api/pets/my-posts`
- **描述**：获取当前用户的发布列表
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```
page=1&limit=10&status=all
```

## 4. 接单模块

### 4.1 接单

- **接口**：`POST /api/orders/accept`
- **描述**：找寻团队接单
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "postId": 1
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "接单成功",
  "data": {
    "orderId": 1
  }
}
```

### 4.2 提交找寻结果

- **接口**：`POST /api/orders/submit-result`
- **描述**：提交找寻结果
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "orderId": 1,
  "result": "found", // found: 找到, not_found: 未找到
  "photos": ["图片URL1"], // 找寻路径图片（未找到时）
  "description": "找寻过程描述"
}
```

### 4.3 我的接单列表

- **接口**：`GET /api/orders/my-orders`
- **描述**：获取当前用户的接单列表
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```
page=1&limit=10&status=all
```

### 4.4 确认找到

- **接口**：`POST /api/orders/confirm-found`
- **描述**：宠物主人确认找到
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "orderId": 1,
  "confirmed": true // true: 确认找到, false: 未找到
}
```

## 5. 支付模块

### 5.1 创建支付订单

- **接口**：`POST /api/payment/create`
- **描述**：创建支付订单
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "postId": 1,
  "amount": 500.0,
  "paymentMethod": "wechat"
}
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "paymentOrderId": "order_123456",
    "paymentUrl": "支付链接",
    "qrCode": "二维码URL"
  }
}
```

### 5.2 支付回调

- **接口**：`POST /api/payment/callback`
- **描述**：支付成功回调
- **请求参数**：根据支付平台规范

### 5.3 查询支付状态

- **接口**：`GET /api/payment/status/{paymentOrderId}`
- **描述**：查询支付状态
- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "paid", // pending: 待支付, paid: 已支付, failed: 支付失败
    "amount": 500.0,
    "paidAt": "2023-01-01T00:00:00Z"
  }
}
```

## 6. 文件上传模块

### 6.1 上传图片

- **接口**：`POST /api/upload/image`
- **描述**：上传图片文件
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：`multipart/form-data`

```
file: 图片文件
type: avatar|pet|id_card|search_path
```

- **响应数据**：

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "url": "https://example.com/images/xxx.jpg",
    "filename": "xxx.jpg",
    "size": 1024000
  }
}
```

### 6.2 批量上传图片

- **接口**：`POST /api/upload/images`
- **描述**：批量上传图片文件
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：`multipart/form-data`

```
files[]: 图片文件数组
type: pet|search_path
```

## 7. 钱包模块

### 7.1 获取钱包余额

- **接口**：`GET /api/wallet/balance`
- **描述**：获取用户钱包余额
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "balance": 1000.0,
    "frozenAmount": 500.0 // 冻结金额
  }
}
```

### 7.2 提现申请

- **接口**：`POST /api/wallet/withdraw`
- **描述**：申请提现
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "amount": 500.0,
  "bankCard": "银行卡号",
  "bankName": "银行名称",
  "accountName": "账户名"
}
```

### 7.3 交易记录

- **接口**：`GET /api/wallet/transactions`
- **描述**：获取交易记录
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```
page=1&limit=10&type=all
```

## 8. 系统模块

### 8.1 获取用户协议

- **接口**：`GET /api/system/terms`
- **描述**：获取用户协议内容

### 8.2 获取隐私政策

- **接口**：`GET /api/system/privacy`
- **描述**：获取隐私政策内容

### 8.3 意见反馈

- **接口**：`POST /api/system/feedback`
- **描述**：提交意见反馈
- **请求头**：`Authorization: Bearer {token}`
- **请求参数**：

```json
{
  "content": "反馈内容",
  "contact": "联系方式",
  "images": ["图片URL1"]
}
```

## 数据库设计要点

### 主要数据表

1. **users** - 用户表
2. **user_verifications** - 实名认证表
3. **lost_pets** - 丢失宠物信息表
4. **orders** - 接单表
5. **payments** - 支付订单表
6. **wallet_transactions** - 钱包交易记录表
7. **feedbacks** - 意见反馈表

### 关键字段

- 所有表包含：`id`, `created_at`, `updated_at`
- 用户相关：`user_id`, `user_type`
- 状态字段：使用枚举值，便于扩展
- 金额字段：使用 DECIMAL 类型，精确到分

## 安全考虑

1. **身份验证**：JWT Token
2. **权限控制**：基于用户类型的接口权限
3. **数据验证**：所有输入参数严格验证
4. **敏感信息**：手机号、身份证号等脱敏处理
5. **文件上传**：限制文件类型、大小，病毒扫描
6. **支付安全**：使用第三方支付平台，避免直接处理支付信息

## 9. 侦探申请模块（新增）

### 9.1 提交/更新侦探申请

- 接口：`POST /api/detective/apply`
- 描述：提交或更新“申请成为宠物侦探”的资料。重复提交视为更新最近一次草稿或被驳回记录。
- 请求头：`Authorization: Bearer {token}`
- 请求参数：

```json
{
  "realName": "张三",
  "phone": "13800138000", // 前端自动带入，后端以token为准
  "city": "上海市",
  "companyName": "某某寻宠团队", // 可为空
  "address": "浦东新区xx路xx号", // 可为空
  "teamSize": 5,
  "devices": ["无人机", "热成像仪", "车辆"],
  "devicePhotos": ["/uploads/devices/a.jpg"],
  "experienceYears": 3,
  "serviceAreas": ["上海市", "苏州市"], // 或者半径配置
  "availableTimes": ["工作日", "周末", "夜间"],
  "bio": "擅长城区夜间搜索与线索排查",
  "idCardFront": "/uploads/id/front.jpg",
  "idCardBack": "/uploads/id/back.jpg",
  "certificates": ["/uploads/cert/b.jpg"], // 可为空
  "payout": {
    // 收款方式（必填）
    "type": "wechat", // wechat | alipay
    "account": "wxid-xxxx" // 或支付宝账号
  },
  "emergencyContact": {
    // 选填
    "name": "李四",
    "phone": "13900000000"
  },
  "agree": true // 必须为 true
}
```

- 响应数据：

```json
{
  "code": 200,
  "message": "提交成功",
  "data": { "applicationId": 123, "status": "pending" }
}
```

### 9.2 获取当前侦探申请与状态

- 接口：`GET /api/detective/application`
- 描述：查询当前登录用户的申请资料与状态。
- 请求头：`Authorization: Bearer {token}`
- 响应数据：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "applicationId": 123,
    "status": "pending", // pending | approved | rejected | draft
    "reason": "资料不完整", // 当 status=rejected 时返回
    "submittedAt": "2025-01-01T10:00:00Z",
    "profile": {
      /* 同 9.1 入参主体 */
    }
  }
}
```

### 9.3 获取侦探申请审核记录

- 接口：`GET /api/detective/application/review-log`
- 描述：返回该用户申请的审核历史（时间线）。
- 请求头：`Authorization: Bearer {token}`
- 响应数据：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "time": "2025-01-01T10:00:00Z",
      "action": "submit",
      "comment": "提交申请"
    },
    {
      "time": "2025-01-01T12:00:00Z",
      "action": "first_review",
      "result": "approved",
      "comment": "资料齐全"
    },
    {
      "time": "2025-01-01T18:00:00Z",
      "action": "final_review",
      "result": "approved",
      "comment": "通过"
    }
  ]
}
```

说明：图片仍使用现有上传接口 `/api/upload/image`；管理端审核与状态回传对齐《管理系统需求.md》中的审核流（初审/复审/回退）。
