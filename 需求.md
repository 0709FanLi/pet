宠物找回平台 - 设计需求文档
概述
本平台为丢失宠物的宠物主人提供信息发布服务。宠物主人可以发布丢失宠物信息，上传照片，提供详细描述，并设置悬赏金额。平台支持微信、电话等方式联系，帮助找到丢失的宠物。以下为设计需求，包含所有页面和元素，确保设计同学可直接使用。
页面总数
共 10 个页面（新增 2 个侦探相关页面），按功能划分如下。

页面设计详情

1. 登录/注册页面

元素：
手机号输入框（占位符：请输入手机号）
验证码输入框（占位符：请输入验证码）+ 发送验证码按钮（右侧，文字：发送验证码）
登录/注册按钮（浅蓝色背景，文字：登录/注册）
协议提示（文字：登录即表示同意《用户协议》和《隐私政策》）

- 点击协议链接弹出弹框显示完整《用户协议》或《隐私政策》内容

功能说明：

- 使用手机号+验证码的方式进行登录和注册
- 如果手机号已注册，则直接登录
- 如果手机号未注册，则自动注册并登录
- 平台支持通过微信、电话等方式联系，帮助寻找宠物
- 用户协议和隐私政策以弹框形式显示，用户点击链接查看完整内容

2. 首页

元素：
发布宠物丢失信息按钮（红色背景，文字：发布丢失信息）
最近发布列表（卡片形式，每卡片含：宠物照片、最后出现地点、支付金额）

3. 发布宠物丢失信息页面

元素：
宠物照片上传区域（支持多张，显示“+”号，提示：请上传宠物照片）
最后出现地点输入框（占位符：请输入最后出现地点
宠物品种输入框（占位符：请输入宠物品种，可选）
宠物名字输入框（占位符：请输入宠物名字，可选）
详细描述文本框（占位符：请输入详细描述，如特征、丢失时间，可选）
输入支付金额（最低 300，用户可手动输入更高金额）
支付方式选择（单选框：微信、支付宝）
发布按钮（蓝色背景，文字：发布并支付）

4. 支付页面

元素：
支付金额显示（文字：支付金额：XXX 元）
支付方式选择（单选框：微信、支付宝，含图标）
支付按钮（绿色背景，文字：立即支付）
提示文字（文字：支付后信息将发布到平台）

5. 发布成功页面

元素：
成功提示（图标：勾号，文字：发布成功！）
查看发布详情按钮（蓝色边框，文字：查看详情）
返回首页按钮（灰色背景，文字：返回首页）

6. 我的发布页面

元素：
导航栏（顶部固定：首页、我的发布、个人中心）
发布历史列表（卡片形式，每卡片含：宠物照片、发布状态、查看详情按钮）
筛选按钮（文字：按状态筛选，下拉：寻找中/已找到/已关闭）

7. 发布详情页面

元素：
宠物照片展示（多张支持滑动）
信息展示区（最后出现地点、宠物品种、宠物名字、详细描述）
支付金额展示（文字：悬赏金额：XXX 元）
联系方式展示（宠物主人联系方式：微信号、电话号码）
分享按钮（蓝色背景，文字：分享寻宠信息，方便转发到朋友圈等社交平台）
状态更新按钮（绿色背景，文字：标记为已找到，找到宠物后可以更新状态）

**侦探专用功能区（仅认证侦探可见）**：
意向状态显示（如：可表达意向/已表达意向/已被确认/您已接单）
意向接单按钮（蓝色背景，文字：意向接单）
撤回意向按钮（橙色背景，文字：撤回意向，表达意向后显示）
联系主人按钮（绿色背景，文字：联系主人，确认接单后显示）

**宠物主人专用功能区（仅发布者可见）**：
意向侦探列表（显示所有表达意向的侦探）

- 侦探基本信息（头像、姓名、经验年限）
- 服务说明和预计完成时间
- 确认授权按钮（文字：选择该侦探）
- 侦探评级展示（可选功能）

8. 个人中心页面

元素：
导航栏（顶部固定：首页、我的发布、个人中心）
用户信息展示（头像、用户名）
编辑个人信息按钮（文字：编辑）
我的发布（文字：查看我的发布记录）
我的接单（文字：查看我的接单记录，仅侦探可见）
帮助与反馈链接（文字：帮助与反馈）
退出登录按钮（红色边框，文字：退出登录）
申请成为宠物侦探（入口按钮/菜单项，展示状态：未申请/审核中/已通过/未通过）
身份状态徽标（通过后显示"侦探已开通"，附"去接单"入口）

## 🕵️‍♂️ 宠物侦探接单系统

### 9. 侦探接单功能

#### 9.1 功能概述

宠物侦探通过审核后，可以在平台上接取寻宠订单，为宠物主人提供专业的寻宠服务。

#### 9.2 权限控制

**侦探身份验证**：

- 只有审核通过的宠物侦探才能看到接单功能
- 普通用户在详情页不显示接单按钮
- 侦探用户在详情页显示"接单"按钮

#### 9.3 接单入口

**入口 1：个人中心 → 去接单**

- 位置：个人中心侦探状态区域
- 条件：侦探审核通过后显示
- 行为：点击跳转到首页宠物丢失列表

**入口 2：首页列表 → 详情页意向接单**

- 位置：寻宠详情页底部
- 条件：当前用户为已认证侦探 AND 该订单状态为"寻找中"
- 行为：显示"意向接单"按钮

#### 9.4 详情页意向接单功能

**页面元素增强**：

- 原有详情页内容保持不变
- 底部新增侦探专用操作区（仅侦探可见）
- 意向接单按钮（蓝色背景，文字：意向接单）
- 撤回意向按钮（橙色背景，文字：撤回意向）
- 联系宠物主人按钮（绿色背景，文字：联系主人，确认接单后显示）

**接单状态显示**：

- 未表达意向：显示"意向接单"按钮
- 已表达意向：显示"已表达意向"+ "撤回意向"按钮
- 已撤回意向：显示"已撤回意向"（置灰，不可再操作）
- 意向数量显示：如"已有 3 位侦探表达意向"
- 已被确认接单：显示"该订单已确认给其他侦探"（置灰）
- 当前侦探被确认：显示"您已接单"+ "联系主人"按钮

#### 9.5 意向接单与确认流程

**第一步：侦探表达意向**

**意向接单弹窗**：

```
标题：表达接单意向
内容：
- 宠物信息摘要（名称、品种、丢失地点）
- 悬赏金额：XXX元
- 预计完成时间选择（1天内/3天内/7天内/其他）
- 服务说明输入框（必填，如：擅长该区域/有相关经验/团队优势等）
- 提示文字：表达意向后，宠物主人可查看您的信息并选择合适的侦探

按钮：
- 表达意向（主按钮）
- 取消（次按钮）
```

**意向表达成功反馈**：

- 成功提示："意向已提交！等待宠物主人确认，您可随时撤回意向"
- 返回详情页，显示"已表达意向"状态

**撤回意向确认**：

```
标题：确认撤回意向
内容：
- 警告文字：撤回后将无法再对该寻宠启示表达意向
- 确认文字：确定要撤回对该订单的意向吗？
- 提示：此操作不可撤销

按钮：
- 确认撤回（警告色按钮）
- 取消（次按钮）
```

**撤回成功反馈**：

- 成功提示："意向已撤回，您无法再对该订单表达意向"
- 返回详情页，显示"已撤回"状态（置灰，不可再操作）

**第二步：宠物主人确认授权**

**宠物主人端新增功能**：

- 在寻宠详情页显示"意向侦探列表"
- 每个侦探显示：头像、姓名、经验、服务说明、预计完成时间
- 侦探评级和历史成功率（可选）
- 确认授权按钮："选择该侦探"

**第三步：确认接单完成**

**侦探端接收确认**：

- 推送通知："您的接单意向已被宠物主人确认！"
- 自动跳转至"我的接单"页面
- 状态变更为"正式接单"，获得宠物主人完整联系方式

**其他侦探处理**：

- 未被选中的侦探收到通知："该订单已被其他侦探接单"
- 自动撤回其他侦探的意向状态

#### 9.6 我的接单管理

**新增页面：我的接单**

- 路由：/pages/detective/orders
- 入口：个人中心"我的接单"菜单项

**页面元素**：

- 接单列表（卡片形式）
  - 宠物照片
  - 宠物基本信息（名称、品种、丢失地点）
  - 悬赏金额
  - 提交时间
  - 订单状态（意向中/已接单/进行中/已完成/已关闭/已撤回）
  - 操作按钮（撤回意向/联系主人/更新进度/完成订单）

**状态筛选**：

- 全部/意向中/已接单/进行中/已完成/已关闭

**状态说明**：

- **意向中**：已表达意向，等待宠物主人确认
- **已接单**：宠物主人已确认，正式接单
- **进行中**：正在执行寻宠任务
- **已完成**：任务完成，宠物找到
- **已关闭**：订单取消或过期
- **已撤回**：主动撤回意向

#### 9.7 订单状态管理

**订单状态流转**：

1. **已发布** → 等待侦探表达意向
2. **有意向** → 侦探表达意向，等待宠物主人确认
3. **已确认** → 宠物主人选择侦探，正式接单
4. **进行中** → 侦探开始寻找，提交进度
5. **已找到** → 宠物找到，等待最终确认
6. **已完成** → 订单完成，悬赏结算
7. **已关闭** → 订单被取消或超时

**可撤回状态**：

- 侦探可在"意向中"状态随时撤回
- 撤回后状态变为"已撤回"，**不可再对同一寻宠启示表达意向**
- 撤回功能为不可逆操作，需二次确认

**进度更新功能**：

- 侦探可以提交寻找进度
- 上传寻找过程照片
- 填写寻找说明
- 宠物主人可查看进度

#### 9.8 通信功能

**联系方式展示**：

- 接单后，侦探可查看宠物主人完整联系方式
- 宠物主人可查看接单侦探的联系方式

**站内消息**（可选扩展）：

- 订单相关消息通知
- 进度更新通知
- 系统通知

#### 9.9 数据库设计

**新增表：detective_orders（侦探订单）**

```sql
- id: 订单ID
- lost_pet_id: 关联寻宠启事ID
- detective_id: 接单侦探ID
- status: 订单状态（intention/confirmed/in_progress/completed/closed/withdrawn）
- intention_at: 表达意向时间
- confirmed_at: 确认接单时间
- estimated_completion: 预计完成时间
- service_description: 服务说明（意向时填写）
- progress_updates: 进度更新记录（JSON）
- completed_at: 完成时间
- withdrawn_at: 撤回时间
- created_at/updated_at: 时间戳
```

**状态字段说明**：

- `intention`: 已表达意向，等待确认
- `confirmed`: 已被宠物主人确认
- `in_progress`: 进行中
- `completed`: 已完成
- `closed`: 已关闭
- `withdrawn`: 已撤回意向

#### 9.10 接口设计

**后端接口**：

**意向相关**：

- `POST /api/detective/orders/intention` - 表达接单意向（需验证是否已撤回）
- `DELETE /api/detective/orders/intention/{id}` - 撤回接单意向（不可逆操作）
- `GET /api/lost-pets/{id}/intentions` - 查询该订单的所有意向
- `GET /api/detective/orders/intention-status/{petId}` - 查询侦探对特定订单的意向状态

**确认相关**：

- `POST /api/lost-pets/{id}/confirm-detective` - 宠物主人确认侦探
- `GET /api/detective/orders/confirmed` - 查询被确认的订单

**订单管理**：

- `GET /api/detective/orders/my` - 我的接单列表（包含意向和正式订单）
- `PUT /api/detective/orders/{id}/progress` - 更新进度
- `PUT /api/detective/orders/{id}/complete` - 完成订单
- `GET /api/lost-pets/{id}/order-status` - 查询订单接单状态

#### 9.11 权限和安全

**权限验证**：

- 接单前验证侦探身份和状态
- 防止重复接单
- 验证订单可接单状态

**数据保护**：

- 联系方式仅接单后可见
- 敏感信息加密存储
- 操作日志记录

#### 9.12 用户体验优化

**状态提示**：

- 实时显示订单接单状态
- 接单人数统计（如"已有 3 人关注"）
- 侦探响应时间显示

**快捷操作**：

- 一键拨打电话
- 一键复制联系方式
- 快速发送常用消息模板

**个性化推荐**：

- 根据侦探服务区域推荐订单
- 根据专长推荐合适的宠物类型
- 历史成功率展示

  8.1 申请成为宠物侦探（个人中心内）

页面目的：

- 用户提交成为“宠物侦探”的申请资料，进入平台审核流程；审核通过后开通侦探权限。

表单分组与字段（V1）：

- 基础信息
  - 真实姓名（必填）
  - 手机号（必填；自动带入，置灰）
  - 所在城市（必填）
  - 公司名称（选填）
  - 常驻地址（选填）
- 团队与设备
  - 团队人数（必填，整数 1–99）
  - 设备清单（必填，多选，可补充“其他”）
    - 推荐：无人机、热成像仪、夜视仪、望远镜、对讲机、寻宠犬、车辆、GPS 追踪器、其他
  - 设备照片（必填，≥1 张，最多 9 张；jpeg/png/jpg，≤10MB/张）
- 能力与经验
  - 经验年限（必填）
  - 服务范围（必填：多选城市或“当前城市+半径”）
  - 可服务时间段（必填：工作日/周末/夜间/节假日）
  - 个人简介/擅长领域（选填）
- 认证与资质
  - 身份证正反面（必填，2 张）
  - 资质/证书/营业执照（选填，0–6 张）
- 联系与结算
  - 收款方式与账号（必填：微信/支付宝账号）
  - 紧急联系人与电话（选填）
- 协议与确认
  - 勾选同意《侦探服务条款》《隐私政策》（必勾）

交互与校验：

- 必填项完整校验；人数为正整数；至少 1 张设备照片；证件照齐全。
- 图片上传沿用 `/api/upload/image`。
- 支持“保存草稿”；状态分为未申请/审核中/已通过/未通过（含驳回原因）。

流程：

- 用户提交 → 待审核 → 管理端初审/复审 → 结果回传（通过开通权限；未通过可修改重提）。

可选扩展（后续版本）：

- 车辆信息、成功案例链接、保险信息、定位授权偏好等。

  8.2 申请审核中页面（新增）

页面路由：/pages/detective/pending

页面目的：

- 在用户提交侦探申请成功后，承接跳转并告知审核时效（不超过 24 小时），减少不确定感。

元素：

- 状态图标（沙漏/加载态）
- 标题文案：申请已提交，等待审核
- 说明文案：我们将在 24 小时内完成审核，请保持电话畅通。如需补充资料可返回后再次进入申请页修改并重新提交。
- 操作按钮：返回首页

交互与流程：

- 申请页提交成功后自动跳转至本页面；
- 后续可在个人中心再次进入“申请成为宠物侦探”进行资料补充并重新提交；
- 管理端完成审核后，前端下次进入个人中心展示“侦探已开通”状态（待接后续联动）。

设计要求补充

导航栏：所有页面顶部固定，含 3 个选项（首页、我的发布、个人中心），当前页面高亮。
底部信息：所有页面含版权信息（文字：© 2023 宠物找回平台）。
风格：简洁、宠物主题（建议使用浅色背景、宠物相关图标）。
表单验证：所有输入框需有占位符和错误提示（如手机号格式错误）。
响应式设计：适配手机、平板、电脑等不同设备屏幕。

---

# 🐾 宠物找回平台 - 设计规范 (Design System)

## 📋 设计理念

**核心价值**: 温暖、可信、高效、关爱
**设计原则**: 简洁友好、情感共鸣、快速响应、无障碍使用

## 🎨 色彩系统 (Color Palette)

### 主色调 (Primary Colors)

- **品牌主色**: `#5B8FF9` (信任蓝) - 体现专业可信赖
- **渐变起点**: `#5B8FF9`
- **渐变终点**: `#36CFC9` (温暖青) - 增加亲和力

### 功能色彩 (Functional Colors)

- **成功**: `#52C41A` (生命绿) - 找到宠物的喜悦
- **警告**: `#FAAD14` (温馨橙) - 友善提醒
- **错误**: `#F5222D` (紧急红) - 重要警示
- **信息**: `#909399` (中性灰) - 辅助信息

### 中性色彩 (Neutral Colors)

- **主文字**: `#303133` (深灰)
- **次文字**: `#606266` (中灰)
- **辅助文字**: `#909399` (浅灰)
- **占位符**: `#C0C4CC` (占位灰)
- **边框**: `#E5E6EB` (边框灰)
- **背景**: `#F8F8F8` (背景灰)

### 情感色彩 (Emotional Colors)

- **温暖橙**: `#FF8C42` (温馨感)
- **爱心粉**: `#FF6B9D` (关爱感)
- **希望黄**: `#FFD93D` (希望感)

## 📝 字体系统 (Typography)

### 字体层级

- **标题 1**: `20px` / `bold` / `#303133` - 页面主标题
- **标题 2**: `18px` / `semibold` / `#303133` - 模块标题
- **标题 3**: `16px` / `medium` / `#303133` - 卡片标题
- **正文**: `14px` / `regular` / `#606266` - 正文内容
- **说明**: `12px` / `regular` / `#909399` - 说明文字
- **标签**: `10px` / `medium` / `#909399` - 状态标签

### 字体规范

- **中文字体**: PingFang SC, Microsoft YaHei, sans-serif
- **英文/数字**: SF Pro, Helvetica Neue, Arial, sans-serif
- **行高**: 1.4-1.6 (确保易读性)

## 🖼️ 图标系统 (Icon System)

### 图标风格

- **风格**: 线性图标为主，填充图标为辅
- **线条粗细**: 1.5px
- **圆角**: 2px (保持统一)

### 核心图标库

- **宠物类**: 🐕 🐱 🐾 (温馨可爱)
- **功能类**: 📍 📞 📷 ⭐ 🔍 (功能明确)
- **状态类**: ✅ ⏳ ❌ ⚠️ (状态清晰)
- **表情类**: 😊 😢 🥺 (情感表达)

## 📐 尺寸规范 (Spacing & Sizing)

### 间距系统 (基于 8px 网格)

- **xs**: `4px` - 最小间距
- **sm**: `8px` - 小间距
- **md**: `16px` - 中等间距
- **lg**: `24px` - 大间距
- **xl**: `32px` - 超大间距

### 圆角规范

- **小圆角**: `4px` - 按钮、输入框
- **中圆角**: `8px` - 卡片内容
- **大圆角**: `12px` - 主要卡片
- **圆形**: `50%` - 头像、图标按钮

### 阴影系统

- **轻阴影**: `0 2px 8px rgba(0, 0, 0, 0.06)` - 卡片悬浮
- **中阴影**: `0 4px 16px rgba(0, 0, 0, 0.08)` - 弹窗组件
- **重阴影**: `0 8px 24px rgba(0, 0, 0, 0.12)` - 模态框

## 🎛️ 组件规范 (Component Specifications)

### 按钮 (Button)

```scss
// 主要按钮
.btn-primary {
  background: linear-gradient(135deg, #5b8ff9 0%, #36cfc9 100%);
  color: #ffffff;
  border-radius: 8px;
  padding: 12px 24px;
  font-size: 16px;
  font-weight: 500;
}

// 次要按钮
.btn-secondary {
  background: #ffffff;
  color: #5b8ff9;
  border: 1px solid #5b8ff9;
  border-radius: 8px;
  padding: 12px 24px;
}

// 文字按钮
.btn-text {
  background: transparent;
  color: #5b8ff9;
  padding: 8px 16px;
}
```

### 卡片 (Card)

```scss
.card {
  background: #ffffff;
  border-radius: 12px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
  overflow: hidden;

  .card-header {
    padding: 16px;
    border-bottom: 1px solid #f0f0f0;
  }

  .card-body {
    padding: 16px;
  }
}
```

### 宠物卡片 (Pet Card)

```scss
.pet-card {
  @extend .card;
  margin-bottom: 16px;

  .pet-image {
    width: 100%;
    height: 180px;
    object-fit: cover;
  }

  .pet-info {
    .pet-location {
      font-size: 16px;
      font-weight: 600;
      color: #303133;
    }

    .pet-reward {
      color: #f5222d;
      font-weight: 600;
    }

    .pet-time {
      color: #909399;
      font-size: 12px;
    }
  }
}
```

### 输入框 (Input)

```scss
.input {
  width: 100%;
  padding: 12px 16px;
  border: 1px solid #e5e6eb;
  border-radius: 8px;
  font-size: 14px;
  color: #303133;

  &::placeholder {
    color: #c0c4cc;
  }

  &:focus {
    border-color: #5b8ff9;
    box-shadow: 0 0 0 2px rgba(91, 143, 249, 0.15);
  }
}
```

## 🏗️ 布局规范 (Layout Guidelines)

### 页面结构

```scss
.page {
  min-height: 100vh;
  background: #f8f8f8;

  .page-header {
    background: #ffffff;
    padding: 16px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }

  .page-content {
    padding: 16px;
    padding-bottom: 100px; // 为TabBar预留空间
  }
}
```

### 栅格系统

- **容器最大宽度**: 750px (移动端)
- **左右边距**: 16px
- **列间距**: 16px

## 🎭 情感化设计 (Emotional Design)

### 空状态设计

```scss
.empty-state {
  text-align: center;
  padding: 60px 20px;

  .empty-icon {
    font-size: 48px;
    margin-bottom: 16px;
    display: block;
  }

  .empty-text {
    color: #909399;
    font-size: 14px;
  }
}
```

### 加载状态

- **骨架屏**: 使用渐变动画，颜色 `#F0F0F0` → `#E0E0E0`
- **Loading 图标**: 宠物爪印旋转动画

### 成功反馈

- **颜色**: 使用生命绿 `#52C41A`
- **图标**: ✅ 或 🎉
- **动画**: 缩放弹出效果

## 📱 适配规范 (Responsive Design)

### 断点设置

- **小屏**: `< 375px` (iPhone SE)
- **中屏**: `375px - 414px` (常规手机)
- **大屏**: `> 414px` (大屏手机/平板)

### 触控区域

- **最小点击区域**: 44px × 44px
- **按钮间距**: 至少 8px
- **滑动手势**: 支持左右滑动和下拉刷新

## 🌟 特殊效果 (Special Effects)

### 爱心动画

```scss
@keyframes heartbeat {
  0%,
  100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
}

.heart-animation {
  animation: heartbeat 1.5s ease-in-out infinite;
}
```

### 宠物轨迹动画

```scss
@keyframes paw-prints {
  0% {
    opacity: 0;
    transform: translateX(-10px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}
```

## 🧩 组件清单 (Component Checklist)

### 基础组件

- [ ] Button (主要/次要/文字/图标)
- [ ] Input (文本/密码/验证码/选择器)
- [ ] Card (宠物卡片/信息卡片)
- [ ] Tag (状态标签/分类标签)
- [ ] Avatar (用户头像/宠物头像)
- [ ] Loading (加载中/骨架屏)
- [ ] Empty (空状态/错误状态)

### 业务组件

- [ ] PetCard (宠物展示卡片)
- [ ] RewardTag (悬赏金额标签)
- [ ] StatusBadge (状态徽章)
- [ ] ImageUploader (图片上传组件)
- [ ] LocationPicker (位置选择器)
- [ ] PhoneInput (手机号输入)
- [ ] CodeInput (验证码输入)

### 页面模板

- [ ] LoginPage (登录模板)
- [ ] ListPage (列表页模板)
- [ ] DetailPage (详情页模板)
- [ ] FormPage (表单页模板)

## 📋 开发规范 (Development Standards)

### CSS 命名规范 (BEM)

```scss
// Block__Element--Modifier
.pet-card__image--rounded {
}
.btn--primary {
}
.form__input--error {
}
```

### 变量使用

```scss
// 优先使用设计token
.component {
  color: $uni-text-color;
  background: $uni-bg-color;
  border-radius: $uni-border-radius-lg;
  padding: $uni-spacing-row-base;
}
```

### 响应式写法

```scss
.responsive-component {
  @media (max-width: 375px) {
    // 小屏样式
  }

  @media (min-width: 415px) {
    // 大屏样式
  }
}
```

## ✅ 设计检查清单

### 页面层面

- [ ] 符合品牌色彩规范
- [ ] 字体大小层级清晰
- [ ] 间距使用 8px 网格
- [ ] 触控区域足够大
- [ ] 错误状态有明确提示
- [ ] 加载状态友好显示

### 组件层面

- [ ] 组件状态完整(默认/悬停/激活/禁用)
- [ ] 边界情况处理(空数据/长文本/网络错误)
- [ ] 无障碍支持(语义化/对比度)
- [ ] 动画流畅自然

### 情感化层面

- [ ] 用词温暖友善
- [ ] 图标符合宠物主题
- [ ] 反馈及时有效
- [ ] 整体体验流畅

---

**最后更新**: 2024 年 12 月  
**适用版本**: fpet uni-app v1.0+
